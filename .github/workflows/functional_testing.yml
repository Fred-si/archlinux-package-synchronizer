name: functional-testing
on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  functional_testing:
    runs-on: archlinux-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install
        run: |
          git status
